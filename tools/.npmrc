# 釘住公開 registry。
#
# 這台開發機的 npm 預設 registry 是公司的 AWS CodeArtifact，
# 若沿用它，產生出來的 package-lock.json 裡每個 resolved URL 都會指向
# 那個私有 registry，CI 上沒有憑證就會 npm ci → E401（實際踩過）。
#
# 這個專案的依賴全是公開套件，而且 APK 是要交給外部的人安裝測試的，
# 建置流程不該綁在公司憑證上。
registry=https://registry.npmjs.org/
