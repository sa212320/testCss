org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# GeckoView 依賴 AndroidX
android.useAndroidX=true

# 明確關掉 predictive back。targetSdk 33+ 若啟用它，Activity.onBackPressed()
# 就不會被呼叫，測試頁的返回鍵會失效 —— 對方要在 6 個頁面之間來回導覽，
# 沒有返回鍵會非常難用。
android.nonTransitiveRClass=true
