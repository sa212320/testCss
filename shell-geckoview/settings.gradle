pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
        // GeckoView 只發布在 Mozilla 自己的 Maven，不在 Maven Central
        maven { url = uri("https://maven.mozilla.org/maven2/") }
    }
}

rootProject.name = "shell-geckoview"
include ':app'
